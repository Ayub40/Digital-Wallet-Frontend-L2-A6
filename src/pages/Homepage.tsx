import { Button } from "@/components/ui/button";

export default function Homepage() {
    return (
        <div className="container mx-auto max-w-screen-xl">
            <Button className='bg-purple-500'>Test Button</Button>
        </div>
    );
}